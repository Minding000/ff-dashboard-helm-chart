apiVersion: v1
kind: Service
metadata:
  name: {{ include "ff-dashboard.fullname" . }}-frontend
  labels:
    {{- include "ff-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.frontend.service.type }}
  ports:
    - port: {{ .Values.frontend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ff-dashboard.frontend.selectorLabels" . | nindent 4 }}
